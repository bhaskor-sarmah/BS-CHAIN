const Wallet = require('./wallet');
const wallet = new Wallet();

console.log(wallet.toString());




/*const Blockchain = require('./blockchain');

const bc = new Blockchain();


for(let i=0; i<50;i++){
    console.log(bc.addBlock(`Bhuju ${i}`).toString());
}
*/